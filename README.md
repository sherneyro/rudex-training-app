# RUDEX Training Bot - Telegram Mini App

## 📱 Что это?

Telegram Mini App для обучения менеджеров компании RUDEX. Современный веб-интерфейс вместо обычных сообщений бота.

## ✨ Преимущества перед n8n-ботом

### Текущий n8n бот:
- ❌ 30+ узлов — сложно поддерживать
- ❌ Длинные сообщения обрезаются
- ❌ Неудобная навигация "назад-вперед"
- ❌ Нет визуального прогресса

### Mini App:
- ✅ **Один веб-интерфейс** вместо 30 узлов
- ✅ **Прокрутка** — любая длина контента
- ✅ **Красивый UI** — карточки, прогресс-бары
- ✅ **Интерактивные тесты** с радио-кнопками
- ✅ **Легче обновлять** — меняешь один HTML файл

## 🚀 Что уже работает

### 1. Главная страница (index.html)
- ✅ 4 модуля с карточками
- ✅ Общий прогресс-бар
- ✅ Разблокировка модулей по порядку
- ✅ Нижнее меню навигации

### 2. Страница модуля (module.html)
- ✅ Вкладки с блоками
- ✅ Прокрутка контента
- ✅ Навигация "предыдущий/следующий"
- ✅ Модуль 1 полностью заполнен контентом

### 3. Страница теста (test.html)
- ✅ 5 вопросов с вариантами ответов
- ✅ Подсчёт правильных ответов
- ✅ Результат с процентами
- ✅ Проходной балл 60%

## 📦 Структура файлов

```
rudex-training-app/
├── index.html          # Главная страница (модули)
├── module.html         # Страница модуля (блоки)
├── test.html           # Страница теста
├── styles.css          # Стили
├── app.js              # Логика приложения
└── README.md           # Этот файл
```

## 🛠 Как развернуть

### Вариант 1: GitHub Pages (БЕСПЛАТНО, 5 минут)

1. **Создай репозиторий на GitHub:**
   ```bash
   # В терминале на своём компьютере
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/rudex-training-app.git
   git push -u origin main
   ```

2. **Включи GitHub Pages:**
   - Зайди в Settings → Pages
   - Source: Deploy from a branch
   - Branch: main → / (root) → Save

3. **Получи ссылку:**
   - `https://ВАШ_ЮЗЕРНЕЙМ.github.io/rudex-training-app/`

### Вариант 2: Vercel (БЕСПЛАТНО, 2 минуты)

1. **Зарегистрируйся на [Vercel](https://vercel.com)**

2. **Импортируй проект:**
   - New Project → Import Git Repository
   - Выбери свой GitHub репозиторий
   - Deploy!

3. **Получи ссылку:**
   - `https://rudex-training-app.vercel.app`

### Вариант 3: Beget (твой хостинг)

1. **Загрузи файлы через FTP:**
   ```
   /domains/tvoy-domen.ru/public_html/training/
   ```

2. **Ссылка:**
   - `https://tvoy-domen.ru/training/`

## 🤖 Подключение к Telegram боту

### Шаг 1: Зарегистрируй Mini App

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду: `/newapp`
3. Выбери своего бота: `@rudex2014_bot`
4. Название: `RUDEX Training`
5. Описание: `Система обучения для менеджеров`
6. Фото: загрузи логотип (512×512 px)
7. **Web App URL:** вставь ссылку на свой хостинг
   - GitHub Pages: `https://ВАШ_ЮЗЕРНЕЙМ.github.io/rudex-training-app/`
   - Vercel: `https://rudex-training-app.vercel.app`
8. Short name: `rudextraining`

### Шаг 2: Добавь кнопку в бота (n8n)

В твоём n8n workflow, где сейчас показывается главное меню, замени на:

**Telegram Node → Send Message:**
```
Text: "Добро пожаловать в систему обучения RUDEX! 🎓"

Reply Markup: inlineKeyboard
Keyboard:
[
  {
    "text": "🚀 Открыть обучение",
    "web_app": { "url": "https://твой-хостинг.ru/training/" }
  }
]
```

### Шаг 3: Тест

1. Открой своего бота в Telegram
2. Отправь `/start`
3. Нажми "🚀 Открыть обучение"
4. Должен открыться Mini App!

## 🔧 Что нужно доделать

### 1. Добавить контент модулей 2-4

Открой `module.html`, найди секцию `moduleData` и добавь контент:

```javascript
const moduleData = {
    // Модуль 1 уже есть ✅
    2: {
        name: 'Продукты RUDEX',
        blocks: {
            1: {
                title: 'Входные двери',
                content: `<h2>🚪 Входные двери</h2>
                         <p>ТВОЙ КОНТЕНТ ЗДЕСЬ</p>`
            }
            // ... остальные блоки
        }
    }
    // Модули 3-4
}
```

### 2. Подключить Google Sheets для сохранения прогресса

Создай серверный скрипт (Node.js или Google Apps Script), который:
- Принимает данные от Mini App
- Сохраняет в Google Sheets
- Возвращает текущий прогресс

**Пример с Google Apps Script:**

```javascript
// В Google Sheets → Extensions → Apps Script
function doPost(e) {
  const data = JSON.parse(e.postData.contents);
  const sheet = SpreadsheetApp.openById('ID_ТВОЕЙ_ТАБЛИЦЫ').getSheetByName('Progress');
  
  if (data.action === 'save_progress') {
    sheet.appendRow([
      data.userId,
      data.moduleId,
      data.blockId,
      new Date(),
      JSON.stringify(data.progress)
    ]);
  }
  
  return ContentService.createTextOutput(JSON.stringify({success: true}));
}
```

Деплой → Web app → Скопируй URL → добавь в `app.js`:

```javascript
async function sendProgressToServer() {
    await fetch('https://script.google.com/macros/s/ТВОЙ_URL/exec', {
        method: 'POST',
        body: JSON.stringify({
            action: 'save_progress',
            userId: userData.userId,
            progress: userData.progress
        })
    });
}
```

### 3. Добавить вопросы для тестов 2-4

Открой `test.html`, секция `testData`, добавь вопросы:

```javascript
const testData = {
    1: { /* уже есть */ },
    2: {
        name: 'Продукты RUDEX',
        questions: [
            {
                question: 'Какие стандартные размеры входных дверей?',
                answers: [
                    { text: '880×2050 и 960×2050', correct: true },
                    // ...
                ]
            }
        ]
    }
    // Тесты 3-4
}
```

## 📊 Как это работает

### Поток пользователя:

1. **Пользователь нажимает кнопку в боте** → открывается Mini App
2. **Главная страница** → выбирает Модуль 1
3. **Страница модуля** → читает 4 блока
4. **Страница теста** → отвечает на 5 вопросов
5. **Результат** → если >60% → открывается Модуль 2
6. Повторяет для модулей 2-4
7. **Сертификат** → после всех модулей

### Как хранится прогресс:

**Локально (пока):**
- `localStorage` в браузере пользователя
- Ключ: `progress_${userId}`

**В будущем (с Google Sheets):**
- Каждое действие → запись в таблицу
- При открытии → загрузка прогресса из таблицы

## 🎨 Кастомизация

### Изменить цвета:

Открой `styles.css`, найди градиенты:

```css
/* Основной цвет (фиолетовый) */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Зелёный (для тестов) */
background: linear-gradient(135deg, #4caf50 0%, #45a049 100%);
```

Замени на цвета RUDEX (например, синий):

```css
background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
```

### Изменить иконки:

В `index.html` и `module.html` замени эмодзи:
- 📖 → 📚
- 🚪 → 🏢
- и т.д.

## 🐛 Решение проблем

### Не открывается Mini App?

1. Проверь URL в BotFather — правильный ли?
2. Открой URL в браузере — работает ли сайт?
3. Проверь консоль браузера (F12) — есть ли ошибки?

### Не сохраняется прогресс?

1. Пока прогресс хранится локально (localStorage)
2. Если очистить данные браузера — прогресс сбросится
3. Нужно подключить Google Sheets (см. выше)

### Mini App не подстраивается под тему Telegram?

Добавь CSS переменные Telegram:

```css
:root {
    --tg-theme-bg-color: var(--tg-theme-bg-color, #ffffff);
    --tg-theme-text-color: var(--tg-theme-text-color, #000000);
}
```

## 📞 Поддержка

Если что-то не работает — пиши мне, разберёмся! 

- Показывай скриншоты ошибок
- Говори, на каком этапе застрял
- Даю подробные инструкции

## ✅ Чеклист запуска

- [ ] Файлы загружены на хостинг
- [ ] URL работает в браузере
- [ ] Зарегистрирован Mini App в BotFather
- [ ] Кнопка добавлена в бота (n8n)
- [ ] Mini App открывается из бота
- [ ] Модуль 1 работает полностью
- [ ] Тест работает
- [ ] Добавлен контент модулей 2-4
- [ ] Добавлены вопросы для тестов 2-4
- [ ] Подключен Google Sheets для прогресса

## 🎉 Результат

После всех шагов у тебя будет:
- ✅ Красивый Mini App вместо обычного бота
- ✅ Удобная навигация
- ✅ Прогресс-бары и статистика
- ✅ Интерактивные тесты
- ✅ Легко обновлять контент

**Удачи! 🚀**
